{
  "name": "@panda-ai/imagequant",
  "version": "0.2.0",
  "module": "index.ts",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Panda-Intelligence/imagequant-wasm.git"
  },
  "scripts": {
    "build": "wasm-pack build --scope panda-ai --target web --release",
    "clean": "rimraf dist",
    "publish": "cd pkg && npm publish --access=public"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}